import React, { Component } from 'react';

const FooterComponent = () => {

    return {
        render() {
            return (
                <div>
                    <footer className="footer">
                        <span className="text-muted">Ovde ide text za footer</span>
                    </footer>
                </div>
            );
        }
    }
}

export default FooterComponent;